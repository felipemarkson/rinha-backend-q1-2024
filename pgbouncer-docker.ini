[databases]
user_db = host=postgres port=5432 dbname=user_db min_pool_size=100 user=user_user

[pgbouncer]
listen_port = 6432
listen_addr = localhost
auth_type = plain
auth_file = userslist.txt
pidfile = pgbouncer.pid
logfile = pgbouncer.log
max_client_conn = 4096
default_pool_size = 100
min_pool_size = 100
